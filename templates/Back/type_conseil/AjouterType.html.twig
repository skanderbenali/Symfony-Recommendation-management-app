{% extends 'base.html.twig' %}

{% block body %}
            <div class="row">

              <div class="col-12 grid-margin stretch-card">
                <div class="card">
                  <div class="card-body">
                    <h4 class="card-title">Basic form elements</h4>
                    <form class="forms-sample">
                      {{form_widget(f)}}
                      <a href="{{path('getAll')}}" class="btn btn-dark">Cancel</a>
                    </form>
                  </div>
                </div>
              </div>
{% endblock %}